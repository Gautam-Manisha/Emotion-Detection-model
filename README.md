# Emotion-Detection-model
This project implements a Facial Emotion Recognition system using Deep Learning techniques. It detects human faces in real time using OpenCV and classifies facial emotions with a Convolutional Neural Network (CNN) built using Keras (TensorFlow backend) in Python.
